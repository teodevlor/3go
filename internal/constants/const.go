package constants

const (
	DefaultPage  = 1     // trang mặc định
	DefaultLimit = 10   // số lượng bản ghi mặc định
	MaxLimit     = 200  // số lượng bản ghi tối đa
	LimitAll     = 10000 // dùng cho filter type=all để lấy toàn bộ bản ghi (không phân trang)
	MaxSearchLen = 200  // giới hạn độ dài search tránh lạm dụng
)
